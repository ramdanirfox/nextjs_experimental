// import type { NextConfig } from "next";


const nextConfig = {
  /* config options here */
  experimental: {
    appDir: true, // Pastikan fitur App Router aktif
  },
};

export default nextConfig;
